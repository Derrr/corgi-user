<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.corgi.mapper.CorgiUserMatchMapper">
    <select id="getGroupMatch" resultType="java.lang.Integer">
        select max(`match`) from corgi_match_group
        where group1 in (${group1}) and group2 = #{group2}
    </select>

    <select id="getCharacterMatch" resultType="java.lang.Integer">
        select sum(`match`) from corgi_match_character
        where ctr1 = #{ctr1} and ctr2 = #{ctr2}
    </select>

    <insert id="addMatchCache">
        insert ignore into corgi_match_cache (`user_id1`, `user_id2`, `match`)
        values (#{userId1}, #{userId2}, #{match});
    </insert>

    <update id="updateMatchCache">
        update corgi_match_cache set `match` = #{match}
        where user_id1 = #{userId1} and user_id2 = #{userId2}
    </update>

    <select id="getMatchCache" resultType="java.lang.Double">
        select `match` from corgi_match_cache
        where (user_id1 = #{userId1} and user_id2 = #{userId2})
        or (user_id2 = #{userId1} and user_id1 = #{userId2})
    </select>

    <select id="getMatchByPage" resultType="com.corgi.user.entity.UserMatch">
        select user_id1 userId1, user_id2 userId2, `match`
        from corgi_match_cache where user_id1 = #{userId} or user_id2 = #{userId}
        limit #{start},#{size}
    </select>
</mapper>