<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.corgi.mapper.CorgiFavorActivityMapper">

    <insert id="addFavor">
        insert ignore into corgi_favor_activity (`user_id`, `activity_id`)
        values (#{userId}, #{activityId});
    </insert>

    <delete id="deleteFavor">
        delete from corgi_favor_activity
        where user_id = #{userId} and activity_id = #{activityId}
    </delete>

    <select id="getActivity" resultType="java.lang.String">
        select activity_id from corgi_favor_activity
        where user_id = #{userId}
        order by id desc
        limit #{start}, #{size}
    </select>

    <select id="countActivity" resultType="java.lang.Integer">
        select count(1) from corgi_favor_activity
        where user_id = #{userId} and activity_id = #{activityId}
    </select>
</mapper>