<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.corgi.mapper.CorgiPushLogMapper">

    <insert id="addPushLog" parameterType="com.corgi.user.entity.PushLog">
        insert into `corgi`.`corgi_push_log` (`from`, `to`)
        values (#{log.from}, #{log.to});
    </insert>

    <select id="countUsefulPush" resultType="java.lang.Long">
        select count(1) from corgi_push_log l
        join corgi_user_position p on p.user_id = l.to
         where l.ctime like concat(#{date},'%') and p.uptime >= #{time}
    </select>

    <select id="countTotalPush" resultType="java.lang.Long">
        select count(1) from corgi_push_log l
        where l.ctime like concat(#{date},'%')
    </select>


</mapper>